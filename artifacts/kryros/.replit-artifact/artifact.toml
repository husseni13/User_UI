kind = "web"
previewPath = "/"
title = "KRYROS"
version = "1.0.0"
id = "artifacts/kryros"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 19796

[services.development]
run = "pnpm --filter @workspace/kryros run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/kryros", "run", "build" ]
publicDir = "artifacts/kryros/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "19796"
BASE_PATH = "/"
